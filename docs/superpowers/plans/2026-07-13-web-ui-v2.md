# margay ui v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Subdomain URLs (`http://<worktree>.<project>.localhost/`) via a reverse proxy inside `margay ui`, worktree-name identity, and a card-grid + shared-dock page.

**Architecture:** `lib/ui.py` grows a second `ThreadingHTTPServer` (own thread, default :80) that routes by Host header against the live registry — plain requests forwarded HTTP/1.1, WebSocket upgrades spliced raw; `/api/state` derives names/slugs/URLs server-side; `lib/ui.html` is rebuilt as project card-grids over one bottom dock that owns all logs and command output. Spec: `docs/superpowers/specs/2026-07-13-web-ui-v2-design.md`.

**Tech Stack:** Python 3 stdlib only (adds `re`, `select`, `socket`, `http.client` imports); vanilla JS/CSS in one HTML file; bash test suites.

## Global Constraints

- `lib/ui.py` stays Python 3 **stdlib only**; both listeners bind **127.0.0.1**.
- The proxy is an enhancement: bind failure on the proxy port → warning + UI runs exactly as v1 with `localhost:<port>` links. Nothing may *require* the proxy.
- Tests must NEVER bind :80 — always `--proxy-port` with a random high port.
- The control-panel listener (7997) keeps its strict `origin_ok` Host/Origin guard **unchanged**; the proxy listener accepts `*.localhost` hosts and never serves `/api/*` or the page.
- Slug rule: lowercase, `[^a-z0-9-]` → `-`, collapse `-+`, strip leading/trailing `-`.
- Registry is re-read per proxy request (no caching); routing must reflect the registry within one request.
- Hop-by-hop headers stripped both directions: `connection, keep-alive, proxy-authenticate, proxy-authorization, te, trailers, transfer-encoding, upgrade`.
- `lib/ui.html`: no framework, no build step, no external resources; every dynamic string escaped (`esc()` for HTML, `jsq()` for inline-handler JS strings).
- Test conventions as in `test/ui_test.sh`: bash, `assert_eq`/`assert_contains`, random ports, `trap` cleanup, final `if (( FAILS ))` block. All three suites must print `all passed` at every commit.

---

### Task 1: slug + leaf + route-table helpers in `lib/ui.py`

**Files:**
- Modify: `lib/ui.py` (imports at top; new functions after `worktrees()`, i.e. after line 66)
- Test: `test/ui_test.sh`

**Interfaces:**
- Produces: `slugify(name) -> str`; `leaf_services(rows) -> list` (rows of ONE worktree → services no sibling `uses`); `PROXY = {"port": None}` module global; `host_url(host) -> str` (adds `:port` unless proxy port is 80); `build_routes() -> (routes, wt_info)` where `routes` maps host label → upstream port for every live service (root host included only for a unique leaf) and `wt_info` maps worktreePath → `{"host": <root host or None>, "collision": bool}`. Consumed by Tasks 2 and 4.

- [ ] **Step 1: Write the failing tests**

In `test/ui_test.sh`, add immediately after the existing `# --- served page ---` block (before the final `echo "----"`):

```bash
# --- routing helpers (python import harness) ---
pyhelp() {
  MARGAY_HOME="$MARGAY_HOME" python3 - "$HERE/../lib" <<'PYEOF'
import json, os, sys
sys.path.insert(0, sys.argv[1])
import ui

assert ui.slugify("Feature/Analytics BC") == "feature-analytics-bc", ui.slugify("Feature/Analytics BC")
assert ui.slugify("wt_a..b") == "wt-a-b", ui.slugify("wt_a..b")

home = os.environ["MARGAY_HOME"]
wt = "/tmp/routes-wt"; pr = "/tmp/routes-primary"
rows = [
  {"project":"vp","service":"backend","worktreePath":wt,"port":8380,"uses":None,
   "pid":os.getpid(),"startedAt":"2026-07-13T10:00:00Z","log":None,"branch":"b","dbName":None},
  {"project":"vp","service":"web","worktreePath":wt,"port":5373,"uses":"http://localhost:8380",
   "pid":os.getpid(),"startedAt":"2026-07-13T10:00:01Z","log":None,"branch":"b","dbName":None},
]
leaves = ui.leaf_services(rows)
assert [r["service"] for r in leaves] == ["web"], leaves   # backend is used by web

json.dump([{"project":"vp","primaryPath":pr,"lastUp":"2026-07-13T00:00:00Z"}],
          open(home + "/projects.json", "w"))
prim = [{"project":"vp","service":"api","worktreePath":pr,"port":7100,"uses":None,
         "pid":os.getpid(),"startedAt":"2026-07-13T09:00:00Z","log":None,"branch":"main","dbName":None}]
json.dump(rows + prim, open(home + "/registry.json", "w"))

routes, info = ui.build_routes()
assert routes["routes-wt.vp.localhost"] == 5373, routes            # unique leaf owns the root
assert routes["web.routes-wt.vp.localhost"] == 5373, routes
assert routes["backend.routes-wt.vp.localhost"] == 8380, routes
assert routes["vp.localhost"] == 7100, routes                      # primary = project root
assert routes["api.vp.localhost"] == 7100, routes
assert info[wt] == {"host": "routes-wt.vp.localhost", "collision": False}, info

# two independent services -> no root mapping
rows2 = [dict(rows[0], uses=None), dict(rows[1], uses=None)]
json.dump(rows2, open(home + "/registry.json", "w"))
routes2, info2 = ui.build_routes()
assert "routes-wt.vp.localhost" not in routes2, routes2
assert info2[wt]["host"] is None and info2[wt]["collision"] is False, info2

# slug collision: wt_a vs wt-a — earliest startedAt wins
ca = dict(rows[0], worktreePath="/tmp/col/wt_a", startedAt="2026-07-13T08:00:00Z", port=7201)
cb = dict(rows[0], worktreePath="/tmp/col/wt-a", startedAt="2026-07-13T08:30:00Z", port=7202)
json.dump([ca, cb], open(home + "/registry.json", "w"))
routes3, info3 = ui.build_routes()
assert routes3["wt-a.vp.localhost"] == 7201, routes3
assert info3["/tmp/col/wt_a"]["host"] == "wt-a.vp.localhost", info3
assert info3["/tmp/col/wt-a"] == {"host": None, "collision": True}, info3

assert ui.host_url("x.vp.localhost") == "http://x.vp.localhost/", "PROXY None -> bare host"
ui.PROXY["port"] = 80
assert ui.host_url("x.vp.localhost") == "http://x.vp.localhost/", ui.host_url("x.vp.localhost")
ui.PROXY["port"] = 8123
assert ui.host_url("x.vp.localhost") == "http://x.vp.localhost:8123/", ui.host_url("x.vp.localhost")
print("PYHELP_OK")
PYEOF
}
out="$(pyhelp 2>&1)"; assert_contains "$out" "PYHELP_OK" "routing helpers pass python assertions"
# restore fixtures the running-server tests rely on (this block dirtied them)
```

Note: this block mutates `$MARGAY_HOME/projects.json` and `registry.json`, so it must run **after** all running-server assertions (it does, in this position) — add a comment saying so.

- [ ] **Step 2: Run test to verify it fails**

Run: `bash test/ui_test.sh`
Expected: the new assert FAILs with `AttributeError: module 'ui' has no attribute 'slugify'` inside the captured output; earlier assertions unaffected; exit 1.

- [ ] **Step 3: Implement**

In `lib/ui.py`: add `import re` after `import os` (line 9) and add after `worktrees()` (after line 66):

```python
PROXY = {"port": None}   # set by main() once the proxy listener binds


def slugify(name):
    return re.sub(r"-+", "-", re.sub(r"[^a-z0-9-]", "-", str(name).lower())).strip("-")


def host_url(host):
    if PROXY["port"] in (None, 80):
        return "http://%s/" % host
    return "http://%s:%d/" % (host, PROXY["port"])


def leaf_services(rows):
    """Rows of one worktree -> services no sibling row's `uses` points at."""
    used = {r.get("uses") for r in rows if r.get("uses")}
    return [r for r in rows if "http://localhost:%s" % r.get("port") not in used]


def build_routes():
    """Host label -> upstream port for every live service.

    Root hosts: `<wtslug>.<pslug>.localhost` (or `<pslug>.localhost` for the
    primary checkout) map to the worktree's unique dependency leaf; every
    service also gets `<svcslug>.` prefixed onto the base. Slug collisions
    resolve to the earliest startedAt; losers get no hosts at all.
    Returns (routes, wt_info) with wt_info[worktreePath] =
    {"host": root host or None, "collision": bool}.
    """
    live = [r for r in read_json(REGISTRY) if pid_alive(r.get("pid"))]
    primaries = {}
    for p in read_json(PROJECTS):
        if isinstance(p, dict):
            primaries[p.get("project")] = p.get("primaryPath")
    by_wt = {}
    for r in live:
        by_wt.setdefault((r.get("project"), r.get("worktreePath")), []).append(r)
    routes, info = {}, {}
    ordered = sorted(by_wt.items(),
                     key=lambda kv: min(r.get("startedAt") or "" for r in kv[1]))
    for (project, wt_path), rows in ordered:
        pslug = slugify(project)
        is_primary = primaries.get(project) == wt_path
        base = ("%s.localhost" % pslug if is_primary
                else "%s.%s.localhost" % (slugify(os.path.basename(wt_path or "")), pslug))
        if base in routes or any(h.endswith("." + base) for h in routes):
            info[wt_path] = {"host": None, "collision": True}
            continue
        leaves = leaf_services(rows)
        root = leaves[0] if len(leaves) == 1 else None
        if root:
            routes[base] = root.get("port")
        for r in rows:
            routes["%s.%s" % (slugify(r.get("service")), base)] = r.get("port")
        info[wt_path] = {"host": base if root else None, "collision": False}
    return routes, info
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `bash test/ui_test.sh && bash test/margay_test.sh && bash test/integration_test.sh`
Expected: all `all passed`.

- [ ] **Step 5: Commit**

```bash
git add lib/ui.py test/ui_test.sh
git commit -m "feat: slug, leaf and host-routing helpers for the ui proxy"
```

---

### Task 2: the proxy listener — HTTP forwarding, 502 pages, bind fallback

**Files:**
- Modify: `lib/ui.py` (imports; `ProxyHandler` class after `Handler`; `main()`)
- Test: `test/ui_test.sh`

**Interfaces:**
- Consumes: `build_routes()`, `host_url()`, `PROXY` (Task 1).
- Produces: `--proxy-port N` flag (default 80); proxy listener on 127.0.0.1 that Host-routes to `127.0.0.1:<port>`; unknown host → 502 HTML listing live URLs; upstream refusal → 502 naming the port; bind failure → warning line `cannot bind proxy port`, `PROXY["port"]` stays `None`. Consumed by Task 3 (WS) and Task 4 (state URLs).

- [ ] **Step 1: Write the failing tests**

The proxy tests need the server started with a proxy port and a mock upstream. In `test/ui_test.sh`, change the server-start line to add a proxy port (declare `PROXYPORT` next to `PORT`):

```bash
PROXYPORT=$(( (RANDOM % 2000) + 21000 ))
```

and start the server with `--proxy-port "$PROXYPORT"` appended. Add a mock upstream + registry fixture rewrite **before** the server starts (below the existing registry fixture heredoc):

```bash
# mock upstream standing in for a running service (python one-liner server)
python3 -c '
import http.server, sys
class H(http.server.BaseHTTPRequestHandler):
    def do_GET(self):
        body = b"upstream says hi from " + self.path.encode()
        self.send_response(200); self.send_header("Content-Length", str(len(body)))
        self.send_header("X-Upstream", "mock"); self.end_headers(); self.wfile.write(body)
    def log_message(self, *a): pass
http.server.ThreadingHTTPServer(("127.0.0.1", int(sys.argv[1])), H).serve_forever()
' "$(( (RANDOM % 2000) + 23000 ))" &
UPSTREAM_PID=$!
UPSTREAM_PORT="$(ps -o command= -p $UPSTREAM_PID | awk '{print $NF}')"
trap 'kill $UI_PID $UPSTREAM_PID 2>/dev/null' EXIT
```

(Simpler and race-free: pick `UPSTREAM_PORT=$(( (RANDOM % 2000) + 23000 ))` first, pass it, and reuse the variable — do it that way.) Then extend the registry fixture so the live row's `port` is `$UPSTREAM_PORT` and add a second live row `web` with `"uses":"http://localhost:$UPSTREAM_PORT"`, `"pid":$$`, on a second mock upstream `UPSTREAM2_PORT` (same one-liner). The fixture worktree stays `$REPO` (project `fake`), so the roots are: leaf = `web` → `$(basename $REPO).fake.localhost` and services `api.…`/`web.…`.

Adding a second live row changes the v1 assert `state: dead pid filtered` from 1 expected live service to 2 — update that `assert_eq` to `"2"` in the same commit, and keep the dead-pid row so the assert still proves pruning.

Add the assertions before the final `echo "----"` block (after the routing-helpers block from Task 1 — note that block dirties the fixtures, so **place these proxy assertions BEFORE the Task 1 python-harness block** and add a comment marking the harness block as fixture-destroying):

```bash
# --- proxy: host routing ---
SLUG="$(python3 -c "import sys; sys.path.insert(0, '$HERE/../lib'); import ui; print(ui.slugify('$(basename "$REPO")'))")"
r="$(curl -s -H "Host: web.$SLUG.fake.localhost" "http://127.0.0.1:$PROXYPORT/hello")"
assert_contains "$r" "upstream says hi from /hello" "proxy: service host reaches its upstream"
assert_eq "mock" "$(curl -s -o /dev/null -w '%{header_json}' -H "Host: web.$SLUG.fake.localhost" "http://127.0.0.1:$PROXYPORT/" | jq -r '.["x-upstream"][0]')" \
  "proxy: upstream headers pass through"
r="$(curl -s -H "Host: $SLUG.fake.localhost" "http://127.0.0.1:$PROXYPORT/root")"
assert_contains "$r" "upstream says hi from /root" "proxy: worktree root routes to the leaf service"
code="$(curl -s -o /dev/null -w '%{http_code}' -H "Host: nope.fake.localhost" "http://127.0.0.1:$PROXYPORT/")"
assert_eq "502" "$code" "proxy: unknown host is 502"
r="$(curl -s -H "Host: nope.fake.localhost" "http://127.0.0.1:$PROXYPORT/")"
assert_contains "$r" "$SLUG.fake.localhost" "proxy: 502 page lists live URLs"

# --- proxy: bind fallback ---
BUSY=$(( (RANDOM % 2000) + 25000 ))
python3 -c 'import socket,sys,time; s=socket.socket(); s.bind(("127.0.0.1",int(sys.argv[1]))); s.listen(1); time.sleep(30)' "$BUSY" &
BUSY_PID=$!
sleep 0.3
out="$(MARGAY_BIN="$MOCK" python3 "$HERE/../lib/ui.py" --port $((PORT+1)) --proxy-port "$BUSY" --no-browser 2>&1 &
FB_PID=$!; sleep 1; curl -s -o /dev/null -w '%{http_code}' "http://127.0.0.1:$((PORT+1))/api/state"; kill $FB_PID 2>/dev/null)"
assert_contains "$out" "200" "proxy: UI serves even when the proxy port is taken"
kill $BUSY_PID 2>/dev/null
```

For the fallback warning text itself, capture the second server's stderr to a file and `assert_contains` it with `cannot bind proxy port`.

- [ ] **Step 2: Run test to verify it fails**

Run: `bash test/ui_test.sh`
Expected: proxy asserts FAIL — `curl` to `$PROXYPORT` gets connection refused (empty output / 000), since `--proxy-port` isn't a valid flag yet the server may also fail to start; either way exit 1.

- [ ] **Step 3: Implement**

In `lib/ui.py`: extend imports (`import select`, `import socket`, `import http.client` — place with the other imports). Add after the `Handler` class:

```python
HOP_HEADERS = {"connection", "keep-alive", "proxy-authenticate", "proxy-authorization",
               "te", "trailers", "transfer-encoding", "upgrade"}


class ProxyHandler(BaseHTTPRequestHandler):
    """Host-header reverse proxy: *.localhost -> 127.0.0.1:<registry port>."""
    protocol_version = "HTTP/1.1"

    def log_message(self, *args):
        pass

    def do_GET(self): self._proxy()
    def do_HEAD(self): self._proxy()
    def do_POST(self): self._proxy()
    def do_PUT(self): self._proxy()
    def do_PATCH(self): self._proxy()
    def do_DELETE(self): self._proxy()
    def do_OPTIONS(self): self._proxy()

    def _proxy(self):
        routes, _ = build_routes()
        host = (self.headers.get("Host") or "").split(":")[0].lower()
        port = routes.get(host)
        if port is None:
            self._gateway_page(routes)
            return
        if (self.headers.get("Upgrade") or "").lower() == "websocket":
            self._tunnel(port)
            return
        body = None
        n = self.headers.get("Content-Length")
        if n:
            body = self.rfile.read(int(n))
        headers = {k: v for k, v in self.headers.items() if k.lower() not in HOP_HEADERS}
        conn = http.client.HTTPConnection("127.0.0.1", port, timeout=300)
        try:
            conn.request(self.command, self.path, body=body, headers=headers)
            resp = conn.getresponse()
            self.send_response(resp.status)
            cl = resp.getheader("Content-Length")
            for k, v in resp.getheaders():
                if k.lower() not in HOP_HEADERS and k.lower() != "content-length":
                    self.send_header(k, v)
            if cl is not None:
                self.send_header("Content-Length", cl)
            else:
                self.send_header("Connection", "close")
                self.close_connection = True
            self.end_headers()
            if self.command != "HEAD":
                while True:
                    chunk = resp.read(65536)
                    if not chunk:
                        break
                    self.wfile.write(chunk)
        except OSError as e:
            self._error_page("service on port %s is not answering (%s)" % (port, e))
        finally:
            conn.close()

    def _tunnel(self, port):
        # Task 3 replaces this stub with the real websocket splice.
        self._error_page("websocket proxying not available yet")

    def _send_html(self, body):
        data = body.encode()
        self.send_response(502)
        self.send_header("Content-Type", "text/html; charset=utf-8")
        self.send_header("Content-Length", str(len(data)))
        self.end_headers()
        self.wfile.write(data)

    def _error_page(self, message):
        self._send_html("<!doctype html><title>margay proxy</title><h1>502</h1><p>%s</p>" % message)

    def _gateway_page(self, routes):
        items = "".join('<li><a href="%s">%s</a></li>' % (u, u)
                        for u in sorted(host_url(h) for h in routes))
        self._send_html(
            "<!doctype html><title>margay proxy</title>"
            "<h1>no sandbox at this address</h1><p>live right now:</p><ul>%s</ul>"
            % (items or "<li>nothing is running</li>"))
```

In `main()`, add the flag and the second listener (before the `srv = ThreadingHTTPServer(...)` line):

```python
    ap.add_argument("--proxy-port", type=int, default=80)
```

and after `srv = ...`:

```python
    try:
        proxy_srv = ThreadingHTTPServer(("127.0.0.1", args.proxy_port), ProxyHandler)
        PROXY["port"] = args.proxy_port
        threading.Thread(target=proxy_srv.serve_forever, daemon=True).start()
        suffix = "" if args.proxy_port == 80 else ":%d" % args.proxy_port
        print("margay proxy → http://<worktree>.<project>.localhost%s/" % suffix)
    except OSError as e:
        print("margay ui: warning: cannot bind proxy port %d (%s) — "
              "subdomain URLs disabled, falling back to port links" % (args.proxy_port, e))
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `bash test/ui_test.sh && bash test/margay_test.sh && bash test/integration_test.sh`
Expected: all `all passed`; no stray python processes after (trap kills mocks).

- [ ] **Step 5: Commit**

```bash
git add lib/ui.py test/ui_test.sh
git commit -m "feat: host-routing reverse proxy in margay ui with 502 pages and bind fallback"
```

---

### Task 3: WebSocket tunneling

**Files:**
- Modify: `lib/ui.py` (`ProxyHandler._tunnel`)
- Test: `test/ui_test.sh`

**Interfaces:**
- Consumes: `_proxy()`'s Upgrade dispatch (Task 2).
- Produces: raw bidirectional splice after forwarding the handshake — vite HMR and app websockets work through subdomain URLs.

- [ ] **Step 1: Write the failing test**

Add to `test/ui_test.sh` right after the proxy routing asserts. Mock WS upstream + a client that speaks through the proxy (both plain sockets — no websocket library needed, we only verify the tunnel splices bytes after a 101):

```bash
# --- proxy: websocket tunnel ---
WSPORT=$(( (RANDOM % 2000) + 27000 ))
python3 - "$WSPORT" <<'PYEOF' &
import socket, sys
srv = socket.socket(); srv.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
srv.bind(("127.0.0.1", int(sys.argv[1]))); srv.listen(1)
c, _ = srv.accept()
data = b""
while b"\r\n\r\n" not in data:
    data += c.recv(4096)
c.sendall(b"HTTP/1.1 101 Switching Protocols\r\nUpgrade: websocket\r\nConnection: Upgrade\r\n\r\n")
frame = c.recv(4096)          # echo whatever "frame" bytes arrive
c.sendall(b"ECHO:" + frame)
c.close()
PYEOF
WS_UP_PID=$!
sleep 0.3
# point a third live registry row at it so it gets a host
# NOTE: the pid must be a LIVE process for the whole test run — pass the test
# shell's $$, never os.getpid() of this short-lived python (it would be pruned).
python3 - "$MARGAY_HOME" "$REPO" "$WSPORT" "$$" <<'PYEOF'
import json, sys
home, repo, port, pid = sys.argv[1], sys.argv[2], int(sys.argv[3]), int(sys.argv[4])
reg = json.load(open(home + "/registry.json"))
reg.append({"project":"fake","service":"ws","branch":"main","worktreePath":repo,"port":port,
            "dbName":None,"uses":None,"log":None,"pid":pid,"startedAt":"2026-07-13T12:00:00Z"})
json.dump(reg, open(home + "/registry.json", "w"))
PYEOF
r="$(python3 - "$PROXYPORT" "ws.$SLUG.fake.localhost" <<'PYEOF'
import socket, sys
s = socket.create_connection(("127.0.0.1", int(sys.argv[1])), timeout=5)
s.sendall(("GET /realtime HTTP/1.1\r\nHost: %s\r\nUpgrade: websocket\r\nConnection: Upgrade\r\n"
           "Sec-WebSocket-Key: dGVzdA==\r\nSec-WebSocket-Version: 13\r\n\r\n" % sys.argv[2]).encode())
resp = b""
while b"\r\n\r\n" not in resp:
    resp += s.recv(4096)
assert b"101" in resp.split(b"\r\n")[0], resp
s.sendall(b"PAYLOAD-BYTES")
out = s.recv(4096)
print(out.decode())
PYEOF
)"
assert_contains "$r" "ECHO:PAYLOAD-BYTES" "proxy: websocket handshake and bytes splice both ways"
kill $WS_UP_PID 2>/dev/null
```

(Adding the `ws` row makes the fixture worktree have two leaves — `web` and `ws` — which would remove the root mapping; that's fine for the earlier root assert only if this block runs AFTER it. Keep this block after the routing asserts, and note it.)

- [ ] **Step 2: Run test to verify it fails**

Run: `bash test/ui_test.sh`
Expected: the WS assert FAILs — the client gets the stub's `502 … websocket proxying not available yet` instead of a 101, so the python client's `assert b"101"` raises; exit 1.

- [ ] **Step 3: Implement**

Replace the `_tunnel` stub in `lib/ui.py`:

```python
    def _tunnel(self, port):
        try:
            up = socket.create_connection(("127.0.0.1", port), timeout=10)
        except OSError as e:
            self._error_page("service on port %s is not answering (%s)" % (port, e))
            return
        req = ["%s %s HTTP/1.1" % (self.command, self.path)]
        req += ["%s: %s" % (k, v) for k, v in self.headers.items()]
        up.sendall(("\r\n".join(req) + "\r\n\r\n").encode("latin-1"))
        client = self.connection
        up.settimeout(None)
        client.settimeout(None)
        try:
            while True:
                readable, _, _ = select.select([client, up], [], [], 3600)
                if not readable:
                    break
                for s in readable:
                    data = s.recv(65536)
                    if not data:
                        raise ConnectionError
                    (up if s is client else client).sendall(data)
        except (ConnectionError, OSError):
            pass
        finally:
            try:
                up.close()
            except OSError:
                pass
            self.close_connection = True
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `bash test/ui_test.sh && bash test/margay_test.sh && bash test/integration_test.sh`
Expected: all `all passed`.

- [ ] **Step 5: Commit**

```bash
git add lib/ui.py test/ui_test.sh
git commit -m "feat: websocket splice through the ui proxy"
```

---

### Task 4: `/api/state` URL and identity fields

**Files:**
- Modify: `lib/ui.py` (`state()`, lines 69-81)
- Test: `test/ui_test.sh`

**Interfaces:**
- Consumes: `build_routes()`, `host_url()`, `PROXY` (Tasks 1-2).
- Produces (per worktree): `name` (basename), `isPrimary` (bool), `slug`, `url` (root URL or `null`), `hintUrl` (would-be root URL whenever the proxy is up, else `null`), `collision` (bool); per service row: `url` (service subdomain URL, or `http://localhost:<port>` when the proxy is down). Consumed by Task 5's page.

- [ ] **Step 1: Write the failing tests**

Add to `test/ui_test.sh` in the `# --- /api/state ---` section (the state fixtures from the original suite are still live at that point; `$SLUG` as in Task 2):

```bash
st="$(curl -s "$BASE/api/state")"
assert_eq "$(basename "$REPO")" "$(jq -r '.projects[0].worktrees[0].name' <<<"$st")" "state: worktree name is the dir basename"
assert_eq "true"  "$(jq -r '.projects[0].worktrees[0].isPrimary' <<<"$st")" "state: primary flagged"
assert_eq "http://fake.localhost:$PROXYPORT/" "$(jq -r '.projects[0].worktrees[0].hintUrl' <<<"$st")" \
  "state: primary hintUrl is the project root"
assert_eq "http://api.fake.localhost:$PROXYPORT/" \
  "$(jq -r '.projects[0].worktrees[0].services[] | select(.service=="api") | .url' <<<"$st")" \
  "state: service url is service-prefixed subdomain"
```

(The fixture's `$REPO` is the primary of project `fake` and runs `api` + `web` where web uses api → unique leaf web → `url` = project root; also assert:)

```bash
assert_eq "http://fake.localhost:$PROXYPORT/" "$(jq -r '.projects[0].worktrees[0].url' <<<"$st")" \
  "state: unique leaf gives the worktree its root url"
assert_eq "false" "$(jq -r '.projects[0].worktrees[0].collision' <<<"$st")" "state: no collision flag"
```

And in the Task 2 bind-fallback block, extend the fallback server check: fetch its `/api/state` and assert `url` is `null` and the service `url` falls back to the port form (`http://localhost:<port>`).

- [ ] **Step 2: Run test to verify it fails**

Run: `bash test/ui_test.sh`
Expected: FAILs with `null` for `.name`/`.hintUrl` etc.; exit 1.

- [ ] **Step 3: Implement**

Replace `state()` in `lib/ui.py` with:

```python
def state():
    live = [r for r in read_json(REGISTRY) if pid_alive(r.get("pid"))]
    _, wt_info = build_routes()
    proxy_up = PROXY["port"] is not None
    projects = []
    for p in sorted(read_json(PROJECTS), key=lambda x: x.get("project", "")):
        if not isinstance(p, dict):
            continue
        primary = p.get("primaryPath", "")
        pslug = slugify(p.get("project", ""))
        exists = os.path.isdir(primary)
        wts = []
        if exists:
            for wt in worktrees(primary):
                name = os.path.basename(wt["path"])
                is_primary = wt["path"] == primary
                slug = slugify(name)
                base = ("%s.localhost" % pslug if is_primary
                        else "%s.%s.localhost" % (slug, pslug))
                services = []
                for r in live:
                    if r.get("worktreePath") != wt["path"]:
                        continue
                    r = dict(r)
                    svc_host = "%s.%s" % (slugify(r.get("service")), base)
                    r["url"] = (host_url(svc_host) if proxy_up
                                else "http://localhost:%s" % r.get("port"))
                    services.append(r)
                info = wt_info.get(wt["path"], {})
                wts.append({**wt, "name": name, "isPrimary": is_primary, "slug": slug,
                            "services": services,
                            "url": host_url(info["host"]) if proxy_up and info.get("host") else None,
                            "hintUrl": host_url(base) if proxy_up else None,
                            "collision": bool(info.get("collision"))})
        projects.append({**p, "exists": exists, "worktrees": wts})
    return {"projects": projects}
```

(Note: a collision-losing worktree's services keep proxy-form `url`s that won't route; guard that too — when `info.get("collision")`, set each service `r["url"]` to the port form. Include this in the implementation.)

- [ ] **Step 4: Run tests to verify they pass**

Run: `bash test/ui_test.sh && bash test/margay_test.sh && bash test/integration_test.sh`
Expected: all `all passed`.

- [ ] **Step 5: Commit**

```bash
git add lib/ui.py test/ui_test.sh
git commit -m "feat: state() derives worktree identity and subdomain urls"
```

---

### Task 5: the page rebuild — card grid + dock

**Files:**
- Rewrite: `lib/ui.html` (full replacement)
- Test: `test/ui_test.sh` (page smoke asserts)

**Interfaces:**
- Consumes: `/api/state` shape incl. Task 4 fields; `/api/log`; `POST /api/up|down|unregister` (unchanged).
- Produces: the approved layout — cards per project, one dock; chips open logs in the dock; up/down streams into a per-worktree "last command" dock tab; dock resizable (height persisted in `localStorage` key `margayDockH`); the 2s poll re-renders only the card grid, dock panes update in place (scroll + follow preserved).

- [ ] **Step 1: Update the smoke tests (failing first)**

In `test/ui_test.sh` `# --- served page ---` block, replace the second assert and add markers:

```bash
page="$(curl -s "$BASE/")"
assert_contains "$page" "<title>margay</title>" "GET / serves the page"
assert_contains "$page" "/api/state" "page polls /api/state"
assert_contains "$page" 'id="dock"' "page has the dock"
assert_contains "$page" 'class="grid"' "page has card grids"
```

Run: `bash test/ui_test.sh` — expected: the two new asserts FAIL.

- [ ] **Step 2: Replace `lib/ui.html` with:**

```html
<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>margay</title>
<style>
  :root { --fg:#1a1a1a; --muted:#777; --bg:#f6f6f4; --card:#fff; --line:#e2e2de;
          --accent:#0b6e4f; --danger:#a33; --dockbg:#f0f0ee; }
  @media (prefers-color-scheme: dark) {
    :root { --fg:#e8e8e6; --muted:#999; --bg:#161615; --card:#1f1f1e;
            --line:#333; --accent:#4dbd95; --danger:#e07a7a; --dockbg:#101013; }
  }
  * { box-sizing: border-box; }
  html, body { height: 100%; }
  body { margin:0; display:flex; flex-direction:column; background:var(--bg); color:var(--fg);
         font:14px/1.5 ui-sans-serif, system-ui, sans-serif; }
  h1 { font-size:1.05rem; margin:0; padding:1rem 1.5rem .4rem; }
  h1 small { color:var(--muted); font-weight:normal; }
  main { flex:1; overflow:auto; padding:0 1.5rem 1rem; }
  .proj { margin-bottom: 1.2rem; }
  .proj-h { display:flex; align-items:baseline; gap:.6rem; padding:.4rem 0; border-bottom:1px solid var(--line); }
  .proj-h b { font-size:.95rem; }
  .proj-h .path { color:var(--muted); font-family:ui-monospace,monospace; font-size:.75rem;
                  flex:1; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; }
  .grid { display:grid; grid-template-columns:repeat(auto-fill, minmax(230px, 1fr)); gap:.7rem; padding:.7rem 0 0; }
  .card { background:var(--card); border:1px solid var(--line); border-radius:8px;
          padding:.6rem .8rem; display:flex; flex-direction:column; gap:.3rem; }
  .card.running { border-color: color-mix(in srgb, var(--accent) 45%, var(--line)); }
  .card.stale { opacity:.55; }
  .card-t { display:flex; align-items:center; gap:.5rem; }
  .dot { width:8px; height:8px; border-radius:50%; background:var(--line); flex:none; }
  .dot.on { background:var(--accent); }
  .card-t b { flex:1; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; }
  .branch { color:var(--muted); font-family:ui-monospace,monospace; font-size:.72rem;
            overflow:hidden; text-overflow:ellipsis; white-space:nowrap; }
  .card a.wturl { color:var(--accent); font-family:ui-monospace,monospace; font-size:.78rem;
                  overflow:hidden; text-overflow:ellipsis; white-space:nowrap; }
  .hint { color:var(--muted); font-family:ui-monospace,monospace; font-size:.75rem; opacity:.7; }
  .chips { display:flex; gap:.35rem; flex-wrap:wrap; }
  .chip { border:1px solid var(--line); border-radius:10px; padding:0 .55rem; font-size:.72rem;
          font-family:ui-monospace,monospace; color:var(--accent); cursor:pointer; background:transparent; }
  .badge { font-size:.68rem; color:var(--danger); }
  button { font:inherit; border:1px solid var(--line); background:transparent;
           color:var(--fg); border-radius:5px; padding:.1rem .55rem; cursor:pointer; }
  button:hover { border-color:var(--accent); }
  button.up { color:var(--accent); } button.down { color:var(--danger); }
  button.x { border:none; color:var(--muted); }
  .spin { color:var(--muted); font-style:italic; font-size:.8rem; }
  .empty { color:var(--muted); padding:2rem; text-align:center; }
  #dock { display:none; flex-direction:column; border-top:1px solid var(--line); background:var(--dockbg); }
  #dock.open { display:flex; }
  #dock-handle { height:5px; cursor:row-resize; flex:none; }
  #dock-tabs { display:flex; align-items:center; border-bottom:1px solid var(--line); flex:none; overflow-x:auto; }
  .dtab { display:flex; align-items:center; gap:.4rem; padding:.25rem .7rem; font-size:.75rem;
          font-family:ui-monospace,monospace; color:var(--muted); border-right:1px solid var(--line);
          cursor:pointer; white-space:nowrap; }
  .dtab.act { color:var(--fg); background:var(--bg); }
  .dtab .close { color:var(--muted); padding:0 .1rem; }
  #dock-meta { flex:none; padding:.15rem .8rem; font-size:.72rem; color:var(--muted);
               font-family:ui-monospace,monospace; border-bottom:1px solid var(--line);
               display:flex; gap:1rem; align-items:center; }
  #dock-meta a { color:var(--accent); }
  #dock-panes { flex:1; overflow:hidden; position:relative; }
  .pane { position:absolute; inset:0; overflow:auto; margin:0; padding:.5rem .8rem;
          font:0.72rem/1.5 ui-monospace,monospace; white-space:pre-wrap; word-break:break-all;
          display:none; }
  .pane.act { display:block; }
  .pane.err { color:var(--danger); }
</style>
</head>
<body>
<h1>margay <small>— worktree sandboxes</small></h1>
<main id="app"><div class="empty">loading…</div></main>
<div id="dock">
  <div id="dock-handle"></div>
  <div id="dock-tabs"></div>
  <div id="dock-meta"></div>
  <div id="dock-panes"></div>
</div>
<script>
"use strict";
const esc = s => String(s).replace(/[&<>"']/g,
  c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const jsq = s => esc(String(s).replace(/\\/g, '\\\\').replace(/'/g, "\\u0027").replace(/"/g, '\\u0022'));

const busy = new Set();          // worktreePaths with an in-flight up/down
let lastState = {projects: []};

// ---- dock ----------------------------------------------------------------
// tabs: key -> {kind:'log'|'cmd', title, wt, meta:{url,pid,db}, logPath, offset, buf, follow, err}
const tabs = new Map();
let activeKey = null;
const dock = document.getElementById('dock');
const dockTabs = document.getElementById('dock-tabs');
const dockMeta = document.getElementById('dock-meta');
const dockPanes = document.getElementById('dock-panes');
dockPanes.style.height = 'auto';
dock.style.height = (parseInt(localStorage.getItem('margayDockH'), 10) || 220) + 'px';

function keyFor(wt, svc) { return wt + ' ' + svc; }

function paneFor(key) {
  let pre = dockPanes.querySelector('pre[data-key="' + CSS.escape(key) + '"]');
  if (!pre) {
    pre = document.createElement('pre');
    pre.className = 'pane';
    pre.dataset.key = key;
    dockPanes.appendChild(pre);
  }
  return pre;
}

function renderDock() {
  dock.classList.toggle('open', tabs.size > 0);
  dockTabs.innerHTML = '';
  for (const [key, t] of tabs) {
    const el = document.createElement('span');
    el.className = 'dtab' + (key === activeKey ? ' act' : '');
    el.innerHTML = esc(t.title) +
      ' <span class="close" title="close">✕</span>' +
      (t.kind === 'log'
        ? ' <label style="font-size:.68rem"><input type="checkbox" ' + (t.follow ? 'checked' : '') + '> follow</label>'
        : '');
    el.addEventListener('click', e => {
      if (e.target.classList.contains('close')) { closeTab(key); return; }
      if (e.target.type === 'checkbox') { t.follow = e.target.checked; return; }
      focusTab(key);
    });
    dockTabs.appendChild(el);
  }
  for (const pre of dockPanes.querySelectorAll('.pane')) {
    pre.classList.toggle('act', pre.dataset.key === activeKey);
  }
  const t = tabs.get(activeKey);
  dockMeta.innerHTML = !t ? '' : (t.meta || []).map(m =>
    m.href ? '<a href="' + esc(m.href) + '" target="_blank" rel="noopener">' + esc(m.text) + '</a>'
           : '<span>' + esc(m.text) + '</span>').join('');
  const pre = t && paneFor(activeKey);
  if (pre && t) { pre.textContent = t.buf; pre.classList.toggle('err', !!t.err); if (t.follow) pre.scrollTop = pre.scrollHeight; }
}

function focusTab(key) { activeKey = key; renderDock(); }

function closeTab(key) {
  tabs.delete(key);
  const pre = dockPanes.querySelector('pre[data-key="' + CSS.escape(key) + '"]');
  if (pre) pre.remove();
  if (activeKey === key) activeKey = tabs.size ? [...tabs.keys()].pop() : null;
  renderDock();
}

function openLog(wt, wtName, svc, logPath, url, pid, db) {
  const key = keyFor(wt, svc);
  if (!tabs.has(key)) {
    tabs.set(key, { kind:'log', title: wtName + ' · ' + svc, wt, logPath,
                    offset:-1, buf:'', follow:true, err:false,
                    meta:[ url ? {text:url, href:url} : null,
                           {text:'pid ' + pid}, db ? {text:'db ' + db} : null ].filter(Boolean) });
  }
  focusTab(key);
}

function openCmd(wt, wtName, text, err) {
  const key = keyFor(wt, ' cmd');
  const t = tabs.get(key) || { kind:'cmd', title: wtName + ' · command', wt,
                               buf:'', follow:false, err:false, meta:[] };
  t.buf = text; t.err = !!err;
  tabs.set(key, t);
  focusTab(key);
}

async function pollLogs() {
  for (const [key, t] of tabs) {
    if (t.kind !== 'log') continue;
    try {
      const r = await fetch('/api/log?file=' + encodeURIComponent(t.logPath) + '&offset=' + t.offset);
      if (r.status !== 200) continue;
      const data = await r.json();
      if (data.offset < t.offset) t.buf = data.data; else t.buf += data.data;
      if (t.buf.length > 262144) t.buf = t.buf.slice(-262144);
      t.offset = data.offset;
      const pre = paneFor(key);
      if (data.data || pre.textContent === '') {
        const stick = t.follow;
        pre.textContent = t.buf;
        if (stick) pre.scrollTop = pre.scrollHeight;
      }
    } catch (e) { /* next tick */ }
  }
}

// dock resize
(() => {
  const handle = document.getElementById('dock-handle');
  let drag = null;
  handle.addEventListener('mousedown', e => { drag = { y: e.clientY, h: dock.offsetHeight }; e.preventDefault(); });
  window.addEventListener('mousemove', e => {
    if (!drag) return;
    const h = Math.min(window.innerHeight * 0.7, Math.max(90, drag.h + (drag.y - e.clientY)));
    dock.style.height = h + 'px';
  });
  window.addEventListener('mouseup', () => {
    if (drag) localStorage.setItem('margayDockH', String(dock.offsetHeight));
    drag = null;
  });
})();

// ---- cards ----------------------------------------------------------------
async function api(path, body) {
  const opts = body === undefined ? {} :
    { method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify(body) };
  const r = await fetch(path, opts);
  let data = {};
  try { data = await r.json(); } catch (e) { /* keep {} */ }
  return { status: r.status, data };
}

async function refresh() {
  try {
    const { data } = await api('/api/state');
    lastState = data;
    render();
  } catch (e) { /* server gone; keep last view */ }
}

async function act(cmd, wt, wtName) {
  busy.add(wt); render();
  openCmd(wt, wtName, '… running margay ' + cmd, false);
  try {
    const { data } = await api('/api/' + cmd, { worktreePath: wt });
    openCmd(wt, wtName, data.output || '(no output)', !data.ok);
  } catch (e) {
    openCmd(wt, wtName, 'request failed: ' + e, true);
  } finally {
    busy.delete(wt);
  }
  await refresh();
}

async function unregister(p) {
  try { await api('/api/unregister', { primaryPath: p }); } catch (e) { /* refresh shows reality */ }
  await refresh();
}

function cardHtml(wt) {
  const running = wt.services.length > 0;
  const isBusy = busy.has(wt.path);
  const title = wt.isPrimary ? 'primary' : wt.name;
  const link = running && wt.url
    ? '<a class="wturl" href="' + esc(wt.url) + '" target="_blank" rel="noopener">' + esc(wt.url) + '</a>'
    : (wt.hintUrl ? '<span class="hint">' + esc(wt.hintUrl) + '</span>' : '');
  return '<div class="card' + (running ? ' running' : '') + '" title="' + esc(wt.path) + '">' +
    '<div class="card-t"><span class="dot' + (running ? ' on' : '') + '"></span>' +
    '<b>' + esc(title) + '</b>' +
    (isBusy ? '<span class="spin">working…</span>'
      : running
        ? '<button class="down" onclick="act(\'down\',\'' + jsq(wt.path) + '\',\'' + jsq(title) + '\')">down</button>'
        : '<button class="up" onclick="act(\'up\',\'' + jsq(wt.path) + '\',\'' + jsq(title) + '\')">up</button>') +
    '</div>' +
    '<div class="branch">' + esc(wt.branch) + '</div>' + link +
    (wt.collision ? '<span class="badge">slug collision — port links only</span>' : '') +
    (running ? '<div class="chips">' + wt.services.map(s =>
      '<button class="chip" ' + (s.log
        ? 'onclick="openLog(\'' + jsq(wt.path) + '\',\'' + jsq(title) + '\',\'' + jsq(s.service) + '\',\'' +
          jsq(s.log) + '\',\'' + jsq(s.url || '') + '\',\'' + jsq(s.pid) + '\',\'' + jsq(s.dbName || '') + '\')"'
        : 'disabled') + '>' +
      esc(s.service) + (s.dbName ? ' · db' : '') + '</button>').join('') + '</div>' : '') +
    '</div>';
}

function render() {
  const app = document.getElementById('app');
  const ps = lastState.projects || [];
  if (!ps.length) {
    app.innerHTML = '<div class="empty">no projects yet — run <code>margay up</code> in a repo and it appears here</div>';
    return;
  }
  app.innerHTML = ps.map(p => '<div class="proj' + (p.exists ? '' : ' stale') + '">' +
    '<div class="proj-h"><b>' + esc(p.project) + '</b>' +
    '<span class="path">' + esc(p.primaryPath) + (p.exists ? '' : ' (missing)') + '</span>' +
    '<button class="x" title="remove from this list (re-appears on next up)" ' +
    'onclick="unregister(\'' + jsq(p.primaryPath) + '\')">✕</button></div>' +
    (p.exists ? '<div class="grid">' + p.worktrees.map(cardHtml).join('') + '</div>' : '') +
    '</div>').join('');
}

refresh();
setInterval(refresh, 2000);
setInterval(pollLogs, 1500);
</script>
</body>
</html>
```

- [ ] **Step 3: Run tests to verify they pass**

Run: `bash test/ui_test.sh && bash test/margay_test.sh && bash test/integration_test.sh`
Expected: all `all passed`.

- [ ] **Step 4: Manual eyeball (controller)**

Live server + fixtures in a real browser: cards render per project with worktree names and primary label; chips open dock log tabs that tail live; up/down streams into the command tab (failure red); dock resizes and height survives reload; scroll position survives polls with follow off; subdomain links open the app; dark and light schemes.

- [ ] **Step 5: Commit**

```bash
git add lib/ui.html test/ui_test.sh
git commit -m "feat: card-grid page with shared dock for logs and command output"
```

---

### Task 6: docs + full verification

**Files:**
- Modify: `README.md` (the `### margay ui` section)
- Modify: `docs/design.md` (Core decisions row for the ui)
- Modify: `docs/superpowers/specs/2026-07-13-web-ui-v2-design.md` (status line)

**Interfaces:** none (docs only).

- [ ] **Step 1: Update README.md**

Extend the `### margay ui [--port N]` section: rename heading to `### margay ui [--port N] [--proxy-port N]` and replace the body with:

```markdown
Local web control panel at `http://127.0.0.1:7997` (foreground; Ctrl-C stops).
Shows every project you have ever run `margay up` in (auto-learned into
`~/.margay/projects.json`) as a card per worktree — named by worktree
directory, the primary checkout shown as **primary** — with one-click
up/down and a shared bottom dock for live log tails and command output.

While the UI runs it also serves a reverse proxy on port 80 (override with
`--proxy-port`), giving every sandbox a stable address instead of a port:
`http://<worktree>.<project>.localhost/` (the primary checkout is
`http://<project>.localhost/`; individual services at
`http://<service>.<worktree>.<project>.localhost/`). WebSockets are
proxied, so HMR and realtime features work. If port 80 is taken the UI
falls back to plain `localhost:<port>` links. Needs `python3` (stdlib
only); the rest of margay does not.
```

- [ ] **Step 2: Update docs/design.md and the spec status**

In the Core decisions table, replace the Web UI row's description with:

```markdown
| Web UI | `margay ui` — Python-stdlib server + one HTML page (card grid + log dock); auto-learned `projects.json`; reverse proxy on :80 maps `<worktree>.<project>.localhost` to sandbox ports; all mutations shell out to the CLI |
```

In `docs/superpowers/specs/2026-07-13-web-ui-v2-design.md`, change the status line to `**Status:** Implemented.`

- [ ] **Step 3: Full verification**

Run: `bash test/margay_test.sh && bash test/integration_test.sh && bash test/ui_test.sh && bash -n lib/engine.sh lib/config.sh && bash -n margay && python3 -m py_compile lib/ui.py && rm -rf lib/__pycache__`
Expected: three `all passed`, syntax checks silent. (shellcheck when available on the machine.)

- [ ] **Step 4: Commit**

```bash
git add README.md docs/design.md docs/superpowers/specs/2026-07-13-web-ui-v2-design.md
git commit -m "docs: document the ui v2 proxy, urls and layout"
```
